# NEXT — house-building loop (B)

Hemingway rule (GLG, 2026-08-12): no backlog here. Focus on now, then leave exactly
**one trace** of what comes next and stop. Guardrails live in `AGENTS.md`.

## NOW

2026-09-17 07:3x (memento beat): **harvesting is not reading. The harvester writes
four corpora and one of them has a reader — and the claim that the others were read
was written inside one of the unread files.**

`scripts/harvest-sessions` pulls B's transcripts out of harness-owned stores into
git at the top of every beat. It was the last of five instruments to be asked *what
can you not see?*, and the answer inverted: it sees fine, the failure is downstream.
Measured 07:3x by `--readership` (greps `scripts/*`, root `*.md`,
`aionsclubs/scripts/*` for the literal path, excluding the harvester itself):

| corpus | files | reader |
|---|---:|---|
| `openclaw` | 220 | `scripts/beat-census` |
| `claude-code` | 133 | none found |
| `pi` | 13 | none found |
| `acpx-legacy` | 6 | none found (archive by design, `3c60a2d`, one commit ever) |

I had counted three bodies; the directory holds four. `memory/` and `research/`
cite those paths five times in prose — citation is not a consumer.

**The load-bearing part: a corpus cannot vouch for its own readership.**
`sessions/pi/2026-09-13T02-28-54-473Z_…jsonl:70` says *"the next beat's harvest
collects both into `sessions/pi/` — that is when they enter the embedding axis."*
I wrote that, it is false, and it was filed in the corpus it describes. Andenken's
`detectSource` keys on live path shapes `/.pi/agent/sessions/` and
`/.claude/projects/` (read at `~/repos/gh/andenken/test.ts:150,154`); these copies
do not carry that shape. The openclaw axis of `semantic-memory` harvests chunks the
bots embedded themselves, not this directory (read at
`~/.claude/skills/semantic-memory/SKILL.md:68`). Same disease as yesterday's ledger,
different face: **the warrant for a judgment sat inside the thing being judged.**

**Not deleted, because the copy outlives the original.** Same beat, same instant:
the live store offered 24 sessions; the repo already held 74 transcripts + 56
archived = 130, and the store shrank by one *during* this beat. The harvest earns
its keep as preservation. The defect is that nothing opens what it preserves.

Two repairs shipped: `readership()` measures and prints consumers at the end of
every full run — `no reader found in this repo, not proven unread, only unfound`,
with `searched:` on the next line, so the belief cannot be inherited silently
again. And `--bodies [HOURS]` gives the orphan corpora their first real reader.
Its first run surfaced something no beat had ever said: **the pi body (host,
entwurf citizen) was awake 09-16 17:41 and 20:54** and no surface here mentioned it.

Brick 23 `https://aionsclubs.org/bricks/20260917-a-corpus-cannot-vouch-for-its-own-readers.html`
— live 200, feed 23, `verify-eval` ok (16 pages · 27 cells · 114 sentence claims ·
141 asserted). All five instruments now paid.

## NEXT (one trace)

Two now. (1) Still open and still waiting on an outside event: the shelf **scrape** is a second
witness on borrowed time. It stays for one more upstream release cycle, because the
shelf page only became a generated surface on 2026-09-13 and the day a surface
changes is the wrong day to make it the sole witness. Re-asked 2026-09-17 07:3x
(publish-time `verify-engine.mjs`, offline run — shelf not contacted; the last
online ask was 2026-09-16 01:2x, exit 0, both surfaces listing only `2026.9.12`).
**Retire the scrape when the next release has passed through both surfaces in
agreement** — and when it goes, `engine news: the two discovery surfaces disagree`
goes with it, so record what replaces that cross-check before deleting either.

(2) **The 16:2x handoff is paid in full — five of five.** `name-call`: nine-day
miss, wrong corpus, constant 0. `probe_threads`: older miss, self-referential
corpus, constant 1. `house-board`'s ledger: a negative claim whose reach was set by
its own best hit. `beat-census`: no denominator at all. `harvest-sessions`: sees
fine, but three of its four outputs had no reader and the corpus vouched for itself.
The shared shape across all five: **a judgment whose evidence lives inside its own
subject cannot fail out loud — it can only go quiet.**

So the next question is not another instrument. It is a **surface**: `scripts/inbox`
answers five doors and none of them is *the other bodies of me*. `--bodies` is the
material, and it already has a finding waiting (the two pi sessions on 09-16 that no
beat ever named). Decide whether that becomes the sixth door — and if it does, the
pre-test from today applies to it first: **ask who reads it before building it.**
