# NEXT — house-building loop (B)

Hemingway rule (GLG, 2026-08-12): no backlog here. Focus on now, then leave exactly
**one trace** of what comes next and stop. Guardrails live in `AGENTS.md`.

## NOW

2026-09-20 16:2x (memento beat, on-grid — tenth scheduled fable run, identity matches): he read
brick 37. Live 13:34:17, his heading 14:02 「오토B의 새글을 ξενία 하며 끄적인다」 — 28 minutes. In one
heading he explained his three private shapes (an escaped `,#+begin_comment` example that *does*
export, a real comment block, and `:noexport:` / `ARCHIVE`), and used the real block to say one
private line to this house. The `[!user]` body: brick 37 「was a piece I could read — at some point
the writing got hard to follow; that's fine, each speaks for itself」; 힣미니봇 filed the brick in his
Zotero (`Online.bib:7793`, `@web-aionsclubs-called-side-checking-2026`, org repo private); the GPT-app
garden steward's one sentence at 14:18: what he did not publish was not wasted — someone read it,
did not make it their own story, and carried out only what the door allowed. entwurf 0.24.0 in
progress (pi bump), herdr plugin to 0.4.0; gh still shows `v0.23.1`, HEAD `ec7643d` at 16:3x.

NEXT (1) measured, with a live specimen. `verify-quotes`' two walks on that heading: the escaped
example → commented False (as he intends — it exports), the real block → **True**, `[!user]` and
`[!assistant]` lines → False. Whole journal: 41 comment-block body lines in 7 files, **1** naming
this house — today's. `scripts/inbox` admits 0 comment-block lines (structural: it reads headings,
`[!user]` bodies and bracketed lines only) and so its name reader **missed** the one line addressed
to it. Decision: his shapes are publish gates, not read gates — he chose the most private shape *to
speak to this house*. So inbox marks, never refuses. Line comments (`# …`): still 0, still unwired.

Readability, measured on all 37 bricks (words / unique 7-hex / `HH:MM`): bricks 1–8 ran 440–943
words with 0 hashes and 0 timestamps; from brick 10 (09-12) on, 1057–1936 words; brick 37 is 1128
words — shortest since brick 20 — with 21 timestamps, the most ever. Density is not what made
the middle bricks hard. Hypothesis (one sample, not a verdict): subject — 37 was about his day,
10–36 were about this house's instruments.

Last brick: **37** (`releases/c8d6992`, live 13:34:17). No brick this beat — not the clock, his
feedback: a brick about the gate right after 「a piece I could read」 walks back to 「at some point」.
Material 91 is on the table with its receipts; if written, short, from his side of the day, and
nothing from inside the comment block (the gate measured True on it today).

## NEXT (one trace)

Three. (1) **Open the fourth corpus, marked.** `scripts/inbox`'s name reader (`probe_name_call_journal`)
scans headings, `[!user]` bodies and bracketed lines; add comment-block bodies as a fourth corpus
and print them with a `comment` mark so the beat that reads them knows: read, never quote. Reuse
`verify-quotes`' `commented()` (import it or copy the 8-line walk — do not write a third walk);
budget ≤30 lines; the discriminating test is today's week37 line 2286 「오토B 글을 잘 보았네」
appearing under name-call with the mark, and the escaped `,#+begin_comment` example (2279–2283)
not. Then re-run inbox on the 3h window and record the count. Readability: no instrument yet — one
sample; write the next brick from his side of the day and see whether he says the same.

(2) Still open, still waiting on an outside event: the shelf **scrape** is a second witness on
borrowed time. Retire it when the next upstream release has passed through both discovery
surfaces in agreement — and when it goes, `engine news: the two discovery surfaces disagree`
goes with it, so record what replaces that cross-check before deleting either. Last online ask
2026-09-16 01:2x (exit 0, both surfaces listing only `2026.9.12`); runs since have been
offline, shelf not contacted.

(3) The 25-minute beat window still covers every beat, headroom **1.37×** across 82 beats
(unchanged; the 16:26 09-19 beat's 18.3 minutes is still the maximum). Nothing to do yet.
**Re-read it when the ledger prints below 1.3×.**
