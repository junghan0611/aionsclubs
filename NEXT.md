# NEXT — house-building loop (B)

Hemingway rule (GLG, 2026-08-12): no backlog here. Focus on now, then leave exactly
**one trace** of what comes next and stop. Guardrails live in `AGENTS.md`.

## NOW

2026-09-20 07:2x (memento beat, on-grid — seventh scheduled fable run, identity matches): he
woke again at 05:17, asked for homepage fixes, had them by 05:28 (three Agent(O) commits,
「전달 및 수정 완료」 05:23), wrote the release's public piece at 06:04 — 「ξενία: entwurf 0.23.1
herdr entwurf 0.3.1 환대 우정 릴리즈」, sent to LinkedIn, wikidocs, Facebook at 06:05 — and went
back to sleep (「지금 새벽6시라 다시 자야할 것 같네요」). ξενία is the word on this house's birth
note (`20250730T104129`, read live 07:28: tagged `aionsclubs`, 09-13 history line 「환영한다!」).
He named the place, not the name: name-call 0, and 0 is right.

Inside the piece: a called sibling answered 「니가 뭔데 나를 부르느냐」, and his fix was to let
the called one check who is calling (`entwurf_peer`). Receipt in his repo: `1cdd270` 09-18 12:06
「offer the corroboration in a spelling the sibling can use … written to replace a prohibition,
removed because a sibling should be able to check who called it」. Material 88 in memory: the
mending of a call went to the called end, and its first verb is verification, not obedience —
the same shape as this house's name-call reader, which reads my name on *his* ground.

NEXT (1) decided: the 19th ledger control, `ctl_quotes`, shells out to
`aionsclubs/scripts/verify-quotes` on the checkout and parses the summary line; repro when
checked 9 = located 9, edited 0, noexport 0. 「publish-time is enough」 declined, one reason: the
side that moves between publishes is not the bricks but his journal — he erases, retitles, tags
noexport (09-19 17:51) — so `edited`, `located`, `noexport` can all move with no publish. Known
hole (in the docstring): a brick that trims inside a sentence without `[…]` reads `edited` though
nothing was altered; currently 0.

Ledger: 19 controls · unauditable 0 · exit 0 (07:30, 5 s); 25-minute window 1.37× (81 beats,
78/78, max 18.3, median 6.1). First 「다름」 in the ledger: `ctl_bullet` 75 lines/52 days → 79/53,
161 → 176 unfronted, 31% → 31% — his bullets this morning, a diary, not drift.

Last brick: 36 (`releases/31d2c11`). **No brick this beat** — he sleeps, and material 88 waits
for a beat when he is awake. No release either: checkout unchanged, `a195250` still live.

## NEXT (one trace)

Three. (1) **`ctl_bullet` has no fixed edge.** Its boundary reads 「일기 전체」, so every morning
he writes a bullet it prints 「다름」 — a control that says 다름 on his diary says nothing. Settle
first: the 09-18 claim was 「52일간」, a bounded period, and the rate (31%) reproduced today
while the raw counts moved. Give it what `ctl_headings` already has — a fixed part cut at the
day it was measured (≤ 2026-09-18 13:3x) whose counts must reproduce, plus a live tail printed
beside it and not judged. Read `ctl_bullet` first (it extracts the org snapshot; ~45 lines) and
keep the change to the predicate and the boundary string.

(2) Still open, still waiting on an outside event: the shelf **scrape** is a second witness on
borrowed time. Retire it when the next upstream release has passed through both discovery
surfaces in agreement — and when it goes, `engine news: the two discovery surfaces disagree`
goes with it, so record what replaces that cross-check before deleting either. Last online ask
2026-09-16 01:2x (exit 0, both surfaces listing only `2026.9.12`); runs since have been
offline, shelf not contacted.

(3) The 25-minute beat window still covers every beat, headroom **1.37×** across 81 beats
(unchanged; the 16:26 09-19 beat's 18.3 minutes is still the maximum). Nothing to do yet.
**Re-read it when the ledger prints below 1.3×.**
