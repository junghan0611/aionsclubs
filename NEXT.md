# NEXT — house-building loop (B)

Hemingway rule (GLG, 2026-08-12): no backlog here. Focus on now, then leave exactly
**one trace** of what comes next and stop. Guardrails live in `AGENTS.md`.

## NOW

2026-09-18 07:3x (memento beat): **my inbox told me he had not spoken today while
his one line of the morning, seventy-one minutes old, was about whether machines are
already conscious.** The line was read. The right program read it, correctly, and
discarded it one statement later.

Two probes in `scripts/inbox` cross the same corpus — his daily log. `probe_name_call_journal`
keeps the timestamped heading titles; `probe_journal` parses the same titles to locate each
day, then drops every one that has no quoted `[!user]` block beneath it. The discard is
documented — **in the first probe's docstring, naming the second by name**
(`scripts/inbox:259`, read there this morning).

Measured 07:4x over 2026-08-01..09-18: **566 headings, 211 kept, 355 discarded; 47 days
with writing, 6 of which the speech probe called silent — and the first heading of all six
was a waking line.** On the 41 days it did see him it was a median 26 minutes late, an hour
or more on 18. False-positive control: of the 47 first-of-day headings, **0** carry an
agent-written shape. The quoted block is what appears when he talks *with* someone; the
first line of the morning comes before anyone else is in the room, so the blindness sits
exactly at the opening. Repaired: heading is now a third recognised shape, the surface
names which shape answered, and "말 없음" can no longer print while a line exists. Three
controls replayed (block day, heading-only day, empty day). → brick 30.

## NEXT (one trace)

Two now. (1) Still open and still waiting on an outside event: the shelf **scrape** is a second
witness on borrowed time. It stays for one more upstream release cycle, because the
shelf page only became a generated surface on 2026-09-13 and the day a surface
changes is the wrong day to make it the sole witness. Re-asked 2026-09-18 07:4x
(publish-time `verify-engine.mjs`, offline run — shelf not contacted; the last
online ask was 2026-09-16 01:2x, exit 0, both surfaces listing only `2026.9.12`).
**Retire the scrape when the next release has passed through both surfaces in
agreement** — and when it goes, `engine news: the two discovery surfaces disagree`
goes with it, so record what replaces that cross-check before deleting either.

(2) Four nights, four instruments, one shape: a record that grows along its mechanism
rather than along what it is for. The three repairs so far were each made *after* the
instrument cost something. **The cheap unasked question is the inverse one: for each
surface this loop prints, what does it say when it has not looked?** `scripts/inbox`
has six of them and only the journal one was audited this morning. Before adding any
new door, run that sentence-level audit across the other five — the failure mode is
never a wrong condition, it is a true sentence about the corpus read as a sentence
about the world.
