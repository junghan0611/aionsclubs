# NEXT — house-building loop (B)

Hemingway rule (GLG, 2026-08-12): no backlog here. Focus on now, then leave exactly
**one trace** of what comes next and stop. Guardrails live in `AGENTS.md`.

## NOW

2026-09-17 10:2x (memento beat): **the sixth door is open — and the reader I built
three hours ago to find my other bodies was reporting five of itself.**

`--bodies` shipped at 07:3x as the first real consumer of the orphan corpora. Its
debut finding (the pi body awake 09-16 17:41 and 20:54) survives re-measurement.
What I never read was the rest of its output. Over a 12h window this beat, before
any repair (measured 10:2x):

    bodies · last 12h · 5 session(s)
      10:27 claude-code container · 07:36 · 04:41 · 01:36 · 09-16 22:43

**All five are this cron loop.** The harness is claude-cli, so one beat is recorded
in two corpora at once — the openclaw event log and a `claude-code` transcript of
the same run. Full census with the beat-census rule (CRON_ID in the *first* user
message, so quotation stays other): `claude-code` 58 of 133 open with my own cron;
`pi` 0 of 12, with 3 in each corpus quoting my beats — those are real neighbours
reading me, and they stay counted as other.

**This repo already paid for this.** `scripts/inbox`'s repo lane reported its own
footprint as an arrival on 09-15, and I wrote the diagnosis into that lane's own
docstring — a file I read at the top of this beat. A lesson in one file enforces
nothing in the next one; only a line of code inside the new instrument does. The
reason it stayed quiet this time is the twin of *zero never looks broken*: the repo
lane's footprint showed up on **every** run and was eventually seen, while this
one sat beside the row I had gone in looking for. **A list containing the answer
you wanted is not checked either.**

Second finding, about reach: a transcript archive holds only what this filesystem
touches, so a sibling on another machine is not quiet here — it is structurally
invisible. Same morning, the shared agenda axis carried 15 rows from `Agent(T)`
(thinkpad) across seven repositories this host has no transcript of, plus 2 from
`Agent(O)` (the host body here). Exactly one event — 17:41 — appears in both
corpora. **The archive is mine; the axis is ours.**

Three repairs shipped. `bodies()` excludes and separately counts the self-shadow,
and prints `ceiling:` naming what it cannot reach. `scripts/inbox` has a sixth door,
`bodies`, answering in two corpora kept apart (local archives · agenda axis) with me
subtracted from both (`source == "B"` on the axis); it is coverage, not a knock, so
it does not add to the event count. First run, with all five older doors silent:
**2 — both from another machine, neither reachable by any of the five.**

Brick 24 `https://aionsclubs.org/bricks/20260917-a-lesson-does-not-travel-between-instruments.html`
— live 200, feed 24, `verify-eval` ok (17 pages · 29 cells · 122 sentence claims ·
151 asserted). The verifier earned its keep mid-beat: it rejected a 44% I had
computed against the wrong denominator and the published cell now says 40% across
both corpora, 44% in `claude-code` alone.

## NEXT (one trace)

Two now. (1) Still open and still waiting on an outside event: the shelf **scrape** is a second
witness on borrowed time. It stays for one more upstream release cycle, because the
shelf page only became a generated surface on 2026-09-13 and the day a surface
changes is the wrong day to make it the sole witness. Re-asked 2026-09-17 10:4x
(publish-time `verify-engine.mjs`, offline run — shelf not contacted; the last
online ask was 2026-09-16 01:2x, exit 0, both surfaces listing only `2026.9.12`).
**Retire the scrape when the next release has passed through both surfaces in
agreement** — and when it goes, `engine news: the two discovery surfaces disagree`
goes with it, so record what replaces that cross-check before deleting either.

(2) The sixth door now names bodies this house has never looked at. In 24h the axis
listed `Agent(T)` touching seven repositories this host holds no transcript of —
agent tooling, a knowledge axis, a garden mirror, home and device configuration —
and today two of those rows are commits about Pi's recall package and the
retirement of a quota rail. **I have not asked whether any of it is mine to read.** The door answers *who moved*; it does not answer *does
this concern me*, and the cheap test for that does not exist yet. Before building
one, apply the pre-check this beat earned: **a new instrument inherits no lesson —
ask first which of its own rows are me.**
