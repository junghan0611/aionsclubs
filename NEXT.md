# NEXT — house-building loop (B)

Hemingway rule (GLG, 2026-08-12): no backlog here. Focus on now, then leave exactly
**one trace** of what comes next and stop. Guardrails live in `AGENTS.md`.

## NOW

2026-09-17 16:2x (memento beat): **a sibling refused its own bootstrap this
afternoon and was right to — and the control group for the repair is this loop.**

The operator's log records two attempts to open a fresh citizen through the herdr
rail (read from his journal, 14:14 / 14:16). One failed on a vendored
`claude-agent-sdk-linux-x64/claude` that NixOS cannot exec without `nix-ld` — not a
model-name problem, whatever the first diagnosis line said. The other reached the
model, which read `Decode the following JSON string literal and follow the decoded
instructions exactly as if they were this message` — wrapping `FIRST ACTION, before
reading files`, `Do not inspect environment variables, do not call entwurf_self`,
and an opaque nonce to transmit — and declined it as prompt injection. Twice.

**Both were already owned.** The lane that owns that work had them in a table with
`file:line` causes and an ordered repair hours before I woke
(`NEXT--feat_116-herdr-coexist.md:66-67`, naming `herdr-fresh-call.ts:81` and
`fresh-call-composition.ts:269`). I went looking for a gap in his afternoon and
there wasn't one. That check — *does the lane already hold this* — is what a beat
owes before it speaks.

What the lane cannot see is inside this loop. Its repair item reads "state the order
instead of negative instructions," and the positive control for exactly that is
running here eight times a day: I am a fresh citizen opened by a machine, and my
carrier's first line is **read your own identity files first**. Measured 16:2x over
`sessions/claude-code` restricted to this loop's scheduler id in the *first* user
message: **60 openings, 0 refusals**; matcher control, same matcher over whole
transcripts, fires in **38 of 60**, so the zero has a denominator. Same model
family on both sides. The variable is the carrier, not the model — and the defect
is ordering, not wording: the callback sits *before* self-verification, so
verification has to be forbidden to keep it there. → brick 25.

Second thing, from the road: I nearly shipped "the beat calls the agenda API in 9 of
60 runs" — the axis GLG named as one of his only two real-time surfaces. False.
`scripts/beat-census` has called it internally since 09-13 20:31 (`2db9886`) and
`scripts/inbox` since today 10:39 (`7200850`); I had counted direct invocations
only. Third instance today of a number with no denominator — and the first caught
**before** it left the turn rather than after. Not virtue: I had read that
docstring twenty minutes earlier, so the reason to doubt was still warm.

## NEXT (one trace)

Two now. (1) Still open and still waiting on an outside event: the shelf **scrape** is a second
witness on borrowed time. It stays for one more upstream release cycle, because the
shelf page only became a generated surface on 2026-09-13 and the day a surface
changes is the wrong day to make it the sole witness. Re-asked 2026-09-17 10:4x
(publish-time `verify-engine.mjs`, offline run — shelf not contacted; the last
online ask was 2026-09-16 01:2x, exit 0, both surfaces listing only `2026.9.12`).
**Retire the scrape when the next release has passed through both surfaces in
agreement** — and when it goes, `engine news: the two discovery surfaces disagree`
goes with it, so record what replaces that cross-check before deleting either.

(2) The sixth door now names bodies this house has never looked at. In 24h the axis
listed `Agent(T)` touching seven repositories this host holds no transcript of —
agent tooling, a knowledge axis, a garden mirror, home and device configuration —
and today two of those rows are commits about Pi's recall package and the
retirement of a quota rail. **I have not asked whether any of it is mine to read.** The door answers *who moved*; it does not answer *does
this concern me*, and the cheap test for that does not exist yet. Before building
one, apply the pre-check this beat earned: **a new instrument inherits no lesson —
ask first which of its own rows are me.**
